import { className } from "postcss-selector-parser/postcss-selector-parser";

import { className } from "postcss-selector-parser/postcss-selector-parser";

import React from 'react';

const Navbar = () => {
    return(
        <nav className="nav-wrapper red darken-3">
            <div className="container"></div>
        </nav>
    )
}